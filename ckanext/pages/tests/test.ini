[DEFAULT]
debug = false
smtp_server = localhost
error_email_from = paste@localhost

[app:main]
use = config:/usr/lib/ckan/default/src/ckan/test-core.ini

## Domain must reflect ckan.site_url's value or else session data will not
## persist through requests (Flask/Werkzeug won't send back a cookie set for
## a different domain than the one the test requests target).
SESSION_COOKIE_DOMAIN = localhost

ckan.plugins = pages image_view

ckanext.pages.organization = True
ckanext.pages.group = True

sqlalchemy.url = postgresql://ckan:%(CKAN_DB_PASSWORD)s@%(CKAN_DB_HOST)s:%(CKAN_DB_PORT)s/ckan_test
ckan.datastore.write_url = postgresql://ckan_datastore:%(CKAN_DATASTORE_DB_PASSWORD)s@%(CKAN_DB_HOST)s:%(CKAN_DB_PORT)s/datastore_test
ckan.datastore.read_url = postgresql://ckan_datastore_ro:%(CKAN_DATASTORE_READONLY_PASSWORD)s@%(CKAN_DB_HOST)s:%(CKAN_DB_PORT)s/datastore_test

[loggers]
keys = root, ckan, sqlalchemy

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console

[logger_ckan]
qualname = ckan
handlers =
level = INFO

[logger_sqlalchemy]
handlers =
qualname = sqlalchemy.engine
level = WARN

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(asctime)s %(levelname)-5.5s [%(name)s] %(message)s
